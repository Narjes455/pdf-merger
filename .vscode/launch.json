{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "PDF Merger: GUI",
      "type": "debugpy",
      "request": "launch",
      "module": "pdf_merger",
      "args": ["--gui"],
      "cwd": "${workspaceFolder}",
      "env": { "PYTHONPATH": "${workspaceFolder}/src" }
    }
  ]
}
